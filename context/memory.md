# Project Memory

Add persistent notes, decisions, and important context here.
The agent reads this file at startup and includes it in every session.

## Example entries

- We use PostgreSQL 15+ (not SQLite) for all databases
- All API endpoints require JWT authentication
- The main branch is protected; always work in feature branches
- Python minimum version is 3.11
